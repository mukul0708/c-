#include <stdio.h>

int main() {
    int number,reminder;
    printf("Enter the number \n");
    scanf("%d",&number);
    reminder=number%2;
    if(reminder==0){
        printf("%d is even",number);
    }
    else{
        printf("%d is odd",number);
    }

    return 0;
}
