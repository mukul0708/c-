#include <stdio.h>

int main() {
    int number,reminder;
    printf("Enter the number \n");
    scanf("%d",&number);
    reminder=number%2;
    if(reminder==0){
        printf("%d is divisible by 2",number);
    }
    else{
        printf("%d is not divisible by 2",number);
    }

    return 0;
}
